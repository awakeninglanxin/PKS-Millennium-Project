好的，根据你之前提出的 2D 轴对称模型和参数敏感度分析需求，我提供一个可直接使用的 Fluent Journal 文件模板 和配套的 Python 参数扫描脚本框架。模板基于 Fluent 的 Scheme (TUI) 语法，兼容 ANSYS Fluent 2021 R1 及以上版本。一、Fluent Journal 文件模板（scan_base.jou）该模板假设你已经准备好：网格文件 mesh_2d_axisym.msh（2D轴对称四边形网格，单位米）UDF 文件 schauberger_sources.c（包含之前给出的三个 DEFINE_SOURCE 函数）模板中使用了字符串 {{PARAM}} 占位符，用于被 Python 脚本替换。; ============================================================; Fluent Journal for 2D Axisymmetric Swirl Simulation; Schauberger "Negative Friction" – Parametric Case; ============================================================; 清空当前会话/file/read-case-dummy; 读取网格/file/read-case mesh_2d_axisym.msh; 单位设置（如果网格以米为单位则跳过）; /units/set length cm; /units/set pressure Pa; 模型设置/define/models/axisymmetric/swirl yes/define/models/viscous/laminar  ; 或选择 transition-sst/define/models/energy no; 材料：水（也可改成空气，只需修改密度和粘度）/define/materials/change-create water liquid water yes  density 998.2  viscosity 0.001003  quit; 边界条件：速度入口（外缘）/define/boundary-conditions/set velocity-inlet inlet  ; 速度大小（取径向分量，负号表示流入）  ; 使用自定义参数 v_in 和 swirl_in  v-mag -{{v_in}}  ; 切向速度（预旋）  swirl-velocity {{swirl_in}}  ; 轴向速度 0  z-velocity 0  ; 湍流（如果层流模型可跳过，否则设置）  turbulence-specification 1  turbulence-intensity 1  viscosity-ratio 10  ; 水力直径（间隙高度）  hydraulic-diameter 0.002  quit; 边界条件：压力出口（中心）/define/boundary-conditions/set pressure-outlet outlet  gauge-pressure 0  backflow-direction 1 0 0  backflow-turb-intensity 1  backflow-viscosity-ratio 10  quit; 边界条件：壁面; 下壁面（静止）/define/boundary-conditions/set wall wall_bottom  material-name water  wall-motion 0  quit; 上壁面（移动，给定转速 omega_disk）/define/boundary-conditions/set wall wall_top  material-name water  wall-motion 1  rotation-axis 0 0 0  speed {{omega_disk}}  quit; 编译和加载 UDF/define/user-defined/compiled-functions compile "schauberger_sources.c"/define/user-defined/compiled-functions load "schauberger_sources.so"; 挂载源项到动量方程; 注意：在 2D axisymmetric swirl 中，坐标顺序为 (z, r, θ); U-momentum (轴向) 挂载 schauberger_source_z/define/user-defined/source-options set-momentum-source 0 (source "schauberger_source_z::libudf"); V-momentum (径向) 挂载 schauberger_source_r/define/user-defined/source-options set-momentum-source 1 (source "schauberger_source_r::libudf"); W-momentum (切向) 挂载 schauberger_source_theta/define/user-defined/source-options set-momentum-source 2 (source "schauberger_source_theta::libudf"); 求解器设置/solve/set/pressure-velocity-coupling 42   ; coupled/solve/set/discretization-scheme 0 1   ; pressure: PRESTO!/solve/set/discretization-scheme 1 2   ; momentum: 2nd order upwind/solve/set/discretization-scheme 2 2   ; swirl: 2nd order upwind/solve/set/discretization-scheme 3 0   ; turbulence: 1st order; 松弛因子（可根据需要调整）/solve/set/under-relaxation/momentum 0.5/solve/set/under-relaxation/swirl 0.7/solve/set/under-relaxation/pressure 0.3; 初始化（标准初始化）/solve/initialize/initialize-flow; 创建监视器; 1. 进出口压差/solve/monitors/force/create pressure-diff monitor  pressure-diff? print? yes  pressure-diff? plot? yes  pressure-diff? write? yes  pressure-diff? file "pressure_diff.out"  ; 定义（需要用户自定义报告定义，这里用 TUI 简化，或通过 GUI）  ; 由于 TUI 复杂，建议用 Python 在计算后从结果文件提取  quit; 2. 轴向力（升力）/solve/monitors/force/create axial-force monitor  name axial-force  print? yes  write? yes  file "axial_force.out"  quit; 设置迭代次数/solve/set/max-iterations 2000/solve/set/convergence-criteria 1e-5; 开始计算/solve/iterate 2000; 保存 case 和 data/file/write-case {{case_name}}.cas/file/write-data {{case_name}}.dat; 输出关键结果到文本文件（以便 Python 提取）/report/summary/print 100  ; 压差（中心与入口）  pressure-diff inlet outlet  ; 轴向力积分（壁面与出口）  force wall_bottom wall_top  quit; 将上述输出重定向到文件/report/summary/write-file {{case_name}}_summary.txt  pressure-diff inlet outlet  force wall_bottom wall_top  quit; 退出 Fluent/exit注意：以上 Journal 中部分 TUI 命令（如设置监视器和报告）在 Fluent 不同版本略有差异，可能需要根据实际版本文法调整。更稳健的做法是直接使用 Scheme 宏 或通过 Python 脚本操纵 Fluent 的 TUI。下面提供的 Python 框架采用后者（直接通过命令行调用 Fluent，并传递参数）。实际使用时，UDF 中的参数（如 alpha, n, C0, C1 等）建议通过 UDS 或环境变量 传入，更简单的方式是写一个头文件 params.h，由 Python 生成不同的 params.h 再编译 UDF。由于 UDF 编译较慢，可采用 全局变量+命名参数 或 用户定义参数（UDF 读取环境变量）。为简化，下面的 Python 脚本直接修改 UDF 源文件中的常数并重新编译。二、参数扫描 Python 脚本框架（param_scan.py）该脚本使用 Python 3 + subprocess，通过替换 Journal 模板中的占位符和 UDF 源文件中的常数，批量运行 Fluent 案例并提取结果。#!/usr/bin/env python3"""Schauberger 2D Axisymmetric Swirl – Parameter Scan Framework用法：  1. 修改下方路径变量（FLUENT_PATH, CASE_DIR 等）。  2. 定义参数扫描网格（param_grid）。  3. 运行脚本：python param_scan.py"""import osimport subprocessimport numpy as npfrom string import Template# ================= 配置文件路径 =================FLUENT_PATH = r"C:\Program Files\ANSYS Inc\v231\fluent\ntbin\win64\fluent.exe"  # Windows# FLUENT_PATH = "/path/to/fluent"  # LinuxJOURNAL_TEMPLATE = "scan_base.jou"          # 上述模板UDF_SOURCE = "schauberger_sources.c"        # 需要修改常数的 UDF 源文件MESH_FILE = "mesh_2d_axisym.msh"OUTPUT_DIR = "./scan_results"               # 存放所有结果STDOUT_LOG_DIR = "./fluent_logs"            # Fluent 运行日志# ================= 参数扫描设置 =================# 定义要扫描的参数列表（每个参数应是一个列表）param_grid = {    "v_in": np.linspace(0.05, 0.5, 20),          # 入口径向速度 [m/s]    "alpha": [31.72],                            # 切割角 [度] （固定为黄金蛋形）    "n_wave": [16],                              # 波纹数    "C0": [0.18],    "C1": np.linspace(0.0, 0.25, 6),            # 负粘性强度    "v_res": [0.255],                            # 谐振速度    "beta4": [2.5e-3],    "A0": [0.001],    "swirl_in": [0.0],                          # 入口预旋（切向/径向比）    "omega_disk": [5.0],                         # 旋转盘角速度 [rad/s]}# 固定参数（不扫描）fixed_params = {    "case_name": "schauberger",   # 将被加上参数标识}# ================= 辅助函数 =================def set_param_in_udf(udf_file, params):    """    修改 UDF 源文件中的全局常量值。    UDF 文件应包含类似:        const real alpha = 31.72;        const real n_wave = 16.0;    该函数用新值替换这些行。    """    with open(udf_file, "r") as f:        content = f.read()    for key, value in params.items():        if key in ["alpha", "n_wave", "C0", "C1", "v_res", "beta4", "A0"]:            # 搜索模式：const real key =            old_line_start = f"const real {key} = "            new_line = f"const real {key} = {value};\n"            # 简单替换（假设每行一个）            for line in content.split("\n"):                if line.strip().startswith(old_line_start.strip()):                    content = content.replace(line, new_line.rstrip())                    break    with open(udf_file, "w") as f:        f.write(content)def generate_journal(template_file, params, output_jou):    """    读取 Journal 模板，替换占位符，生成新的 Journal 文件。    """    with open(template_file, "r") as f:        template_content = f.read()    # 使用 string.Template 替换    t = Template(template_content)    rendered = t.safe_substitute(params)    with open(output_jou, "w") as f:        f.write(rendered)def run_fluent(jou_file, case_dir, log_file):    """    调用 Fluent 批量运行 Journal。    """    cmd = [        FLUENT_PATH,        "2d",                       # 2D 模式        "-g",                       # 无 GUI        "-i", jou_file,             # Journal 文件        "-t0",                      # 单线程（调整线程数）        "-cwd", case_dir,           # 工作目录    ]    with open(log_file, "w") as f:        proc = subprocess.run(cmd, stdout=f, stderr=subprocess.STDOUT,                               shell=False, cwd=case_dir)    return proc.returncodedef extract_results(case_dir, case_name):    """    从 Fluent 输出的 summary 文件中提取压差和轴向力。    返回字典。    """    summary_file = os.path.join(case_dir, f"{case_name}_summary.txt")    if not os.path.exists(summary_file):        return None    # 这里需要根据 summary 文件的格式解析关键数值    # 简化：假设 summary 文件包含 "pressure-diff: 12.345 Pa" 之类的行    # 示例解析（实际需根据输出调整）    results = {"dp": None, "axial_force": None}    with open(summary_file, "r") as f:        for line in f:            if "pressure-diff" in line.lower() or "pressure difference" in line.lower():                # 尝试提取数字                parts = line.split()                for p in parts:                    try:                        results["dp"] = float(p)                        break                    except:                        pass            if "force" in line.lower() and "wall_bottom" in line.lower():                # 下一行可能有数值                continue            # 更精确的解析需要根据实际输出格式    return results# ================= 主扫描循环 =================def main():    os.makedirs(OUTPUT_DIR, exist_ok=True)    os.makedirs(STDOUT_LOG_DIR, exist_ok=True)    # 生成所有参数组合（网格不很大时直接循环）    import itertools    keys = list(param_grid.keys())    values = list(param_grid.values())    total_cases = np.prod([len(v) for v in values])    print(f"Total cases to run: {total_cases}")    # 复制网格和 UDF 到输出目录（或使用绝对路径）    # 这里假设网格和 UDF 在当前目录    case_index = 0    for combo in itertools.product(*values):        params = dict(zip(keys, combo))        # 添加固定参数        case_label = f"v_{params['v_in']:.3f}_C1_{params['C1']:.3f}"        case_dir = os.path.join(OUTPUT_DIR, f"case_{case_index:04d}_{case_label}")        os.makedirs(case_dir, exist_ok=True)        # 复制网格和 UDF 到 case_dir（或使用相对路径，做好文件组织）        # 这里假定 Fluent 启动时工作目录在 case_dir，而网格和 UDF 源文件在上级目录        # 先复制必要文件到 case_dir        shutil.copy(MESH_FILE, case_dir)        # 需要 import shutil        shutil.copy(UDF_SOURCE, case_dir)        # 1. 修改 UDF 源文件中的常数        udf_params = {k: params[k] for k in ["alpha", "n_wave", "C0", "C1", "v_res", "beta4", "A0"] if k in params}        udf_path = os.path.join(case_dir, UDF_SOURCE)        set_param_in_udf(udf_path, udf_params)        # 2. 生成 Journal 文件        jou_params = {            "v_in": params["v_in"],            "swirl_in": params["swirl_in"],            "omega_disk": params["omega_disk"],            "case_name": case_label,   # 用于保存 case/data        }        jou_path = os.path.join(case_dir, f"run_{case_label}.jou")        generate_journal(JOURNAL_TEMPLATE, jou_params, jou_path)        # 3. 运行 Fluent        log_file = os.path.join(STDOUT_LOG_DIR, f"case_{case_index:04d}.log")        ret = run_fluent(jou_path, case_dir, log_file)        # 4. 提取结果        results = extract_results(case_dir, case_label)        if results:            # 写入汇总 CSV            csv_line = f"{case_index},{params['v_in']},{params['C1']},{params['alpha']},{results['dp']},{results['axial_force']}\n"            csv_file = os.path.join(OUTPUT_DIR, "results_summary.csv")            with open(csv_file, "a") as f:                f.write(csv_line)        case_index += 1        print(f"Finished case {case_index}/{total_cases}: {case_label}, returncode={ret}")if __name__ == "__main__":    # 需要先 import shutil    import shutil    main()用法说明：将上述两个代码块保存为 scan_base.jou 和 param_scan.py。准备网格文件 mesh_2d_axisym.msh 和 UDF 源文件 schauberger_sources.c（内容为你之前给出的三方向源项，且包含可替换的常数定义，例如 const real alpha = 31.72;）。修改 FLUENT_PATH 为你的 Fluent 可执行文件路径。在 Python 环境中运行 python param_scan.py。结果会保存在 ./scan_results/results_summary.csv 中，包含参数组合与对应的压差和轴向力。三、UDF 源文件修改建议为了让 Python 脚本能自动修改常数，UDF 源文件的开头应类似：#include "udf.h"/* 可调参数（会被脚本替换） */const real alpha = 31.72;          // 切割角 [度]const real n_wave = 16.0;          // 波纹数const real C0 = 0.18;              // 基础粘性放大系数const real C1 = 0.17;              // 负粘性强度const real v_res = 0.255;          // 谐振速度 [m/s]const real beta4 = 2.5e-3;         // 轴向耦合系数const real A0 = 0.001;             // 波纹幅度 [m]然后保留三个 DEFINE_SOURCE 函数不变（它们读取这些全局变量）。四、扩展与改进建议使用 PyFluent：ANSYS 2021 R2 以上提供 ansys.fluent.core，可用 Python 直接控制求解器，无需 Journal 文件。若你安装有 PyFluent，推荐使用，效率更高且易于错误处理。并行计算：可通过 -t4 参数调用 4 核并行，或使用多进程 Python 同时运行多个 Fluent 实例（注意许可证限制）。后处理自动化：利用 report/summary 输出结果的同时，可通过 Fluent 的 TUI 命令将监视器数据写入独立文件，便于 Python 提取。参数扫描替代方法：若参数组合过多，可采用 拉丁超立方采样 或 贝叶斯优化 以减少运行次数，尤其在探索大范围参数时。如果需要进一步细化（如适配具体版本的 Fluent TUI、添加并行运行支持、或改用 PyFluent 示例），请告诉我。
