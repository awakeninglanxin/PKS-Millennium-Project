# v8 DEM+几何XOR — 四变体参数对比

> 日期：2026-07-29 | GPU：RTX 4090 | 6000×6000px | DEM 400轮

---

## 算法架构

```
朝内（逃逸区）：DEM -log(d) 边界光晕
朝外（有界区）：几何XOR棋盘 + 密度指数梯度 + 距离衰减
```

**几何XOR**（极坐标）：

$$
\operatorname{chess} = (\lfloor\theta\cdot360\rfloor \bmod 2) \oplus (\lfloor\ln(|c|+0.01)/\Delta(d)\rfloor \bmod 2)
$$

**核心参数 $\Delta(d)$** —— 棋盘步长，控制密度梯度：

$$
\boxed{\Delta(d) = \Delta_0 \times e^{K \cdot d/10}}
$$

三个变量：$\Delta_0$（边界起步密度）、$K$（指数增长速率）、$d$（DEM距离）。

---

## 四变体参数

| 变体 | 文件名 | $\Delta_0$ | $K$ | 边界Δ | 远处Δ(10) | 比值 |
|:---|:---|:---|:---|:---|:---|:--:|
| **A** | `v8_Δ=0.003e^8.37d.png` | 0.003 | 8.37 | 0.003 | 13.0 | **4334×** |
| **B** | `v8_Δ=0.001e^9.5d.png` | 0.001 | 9.5 | 0.001 | 13.4 | **13360×** |
| **C** | `v8_Δ=0.01e^7.0d.png` | 0.01 | 7.0 | 0.01 | 11.0 | **1097×** |
| **D** | `v8_Δ=0.003e^6.0d.png` | 0.003 | 6.0 | 0.003 | 1.2 | **403×** |
| **E** | `v8_Δ=0.1e^7.0d.png` | 0.1 | 7.0 | 0.1 | 110 | **1097×** |

**解读**：
- **A**（当前默认）：边界极密(0.003)，远处急速稀疏(13.0)，4334倍跨距
- **B**：边界更密(0.001)，远处稀疏程度与A相当，但跨距更大(13360倍)
- **C**：边界较粗(0.01)，远处与A接近，但近边界密度低了3倍
- **D**：与A同样的边界密度，但远处只到1.2，棋盘蔓延更远

---

## 完整工序

### 1. GPU DEM迭代
- 逆M映射 $c_{\text{eff}} = 1/c$
- 迭代 $z_{n+1} = z_n^2 + c_{\text{eff}}$，追踪 $dz_{n+1} = 2z_n \cdot dz_n + 1$
- DEM距离：$d = \ln|z_n|^2 \cdot |z_n| / |dz_n|$
- 400轮，bailout=50

### 2. 朝内（escaped区域）
- $s_{\text{in}} = \operatorname{clip}(-\log d / 5,\; 0,\; 1)$
- 边界最亮，向内自然衰减

### 3. 朝外（bounded区域）
- 极角 $\theta = (\arctan2(y,x) + \pi) / 2\pi$
- 对数距离 $r = \ln(|c| + 0.01)$
- 梯度步长 $\Delta(d) = \Delta_0 \cdot e^{K \cdot d/10}$
- XOR棋盘 + 高斯柔化($\sigma$=0.8)
- 衰减权重 $w = e^{-4d/10}$（边界=1，远处→0）
- $s_{\text{out}} = \operatorname{clip}(\text{chess}_{\text{soft}} \cdot w,\; 0,\; 1)$

### 4. Plasma着色 + 旋转90°

---

## 文件清单

| 文件 | 说明 |
|:---|:---|
| `v8_dem_sign.py` | 主脚本 |
| `v8_Δ=0.003e^8.37d.png` | 变体A（当前默认） |
| `v8_Δ=0.001e^9.5d.png` | 变体B |
| `v8_Δ=0.01e^7.0d.png` | 变体C |
| `v8_Δ=0.003e^6.0d.png` | 变体D |
| `v8_数学原理.md` | 本文档 |

---

> ⚠️ **重要声明 / Important Disclaimer**
> 
> 本文档由 AI 辅助生成，部分结论可能存在 AI 幻觉导致的论证不严谨之处。
> 文中提出的数学、物理及相关跨学科观点，需要经过专业数学家、物理学家
> 及相关领域专家共同验证与检验。
> 如有疏漏、错误或不同见解，敬请指正，不胜感激。
> 
> **This document was AI-assisted. Some conclusions may contain inaccuracies
> due to AI hallucination. All mathematical, physical, and interdisciplinary
> claims require verification by professional mathematicians, physicists,
> and subject-matter experts. Corrections and feedback are warmly welcomed.**
