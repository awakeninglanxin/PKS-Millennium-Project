# SEG / IGV 多层参数化几何引擎 · 完整说明

---

## 一、几何模型总览

瑟尔效应发电机（SEG）和反重力装置（IGV）由三组同心环构成，从内到外依次为：

```
        定子环1 → 滚筒组1 → 定子环2 → 滚筒组2 → 定子环3 → 滚筒组3
        (inner)              (middle)              (outer)
```

每环定子为环形截面（内径 Brᵢ，外径 BRᵢ），每环滚筒在对应轨道半径 orbitᵢ 上公转。

---

## 二、基础物理公式（来源：Wolfram nb 程序 + 文档规格）

### 2.1 滚筒几何

| 参数 | 公式 | 说明 |
|---|---|---|
| 滚筒直径 D | `D = 2 × Yr` | Yr 为滚筒半径 |
| 滚筒高度 Yh (默认) | `Yh = D / tan(22.5°)` | 文档核心公式，tan(22.5°)≈0.4142 |
| 滚筒高度 Yh (k1约束) | `Yh ≤ Bh / k1`（k1=1~1.5, 默认1.2） | 比例约束 |
| 滚筒高度 Yh (k2约束) | `Yh = max(1, Bh − 2×k2)`（k2=0~9cm, 默认1cm） | 间隙约束，滚筒距定子上下各 k2 cm |
| 滚筒体积 Yv | `Yv = π × Yr² × Yh` | 圆柱体积 |
| 滚筒片 | 每滚筒=8片，片质量可调 1~120g，默认 34g | 8×34=272g/颗 |

### 2.2 定子环几何

| 参数 | 公式 | 说明 |
|---|---|---|
| 定子外半径 BRᵢ | `BRᵢ = Yr × numᵢ × n / 2`（方案1~5,7,8） | 文档：外直径=直径×滚筒数×n/2 |
| | `BRᵢ = (2×Yr + n) / (2×sin(π/numᵢ))`（方案6专用） | 正多边形弦长公式 |
| 定子内半径 Br₀ | `Br₀`（最内环） | 用户可调，范围 0~2000cm，默认 0 |
| 定子高度 Bhᵢ | `Bhᵢ = D × e` (size方案) | e 为用户可调系数，默认 2.4 |
| 定子体积 Bvᵢ | `Bvᵢ = π × (BRᵢ² − Brᵢ²) × Bhᵢ` | 环形体积 |
| 环厚度 tᵢ | `tᵢ = BRᵢ − Brᵢ` | 方案4/5 要求等厚 |

### 2.3 间隙与嵌套

| 参数 | 公式 | 说明 |
|---|---|---|
| 环间距 gapᵢ | `gapᵢ = Brᵢ₊₁ − BRᵢ` | 第 i 环和第 i+1 环之间的空隙 |
| 间隙约束 | `gapᵢ ≥ 2×Yr + clearanceGap` | 滚筒直径 + 额外间隙（默认 1cm，下限 1cm） |
| 轨道半径 orbitᵢ | `orbitᵢ = (BRᵢ + Brᵢ₊₁) / 2` | 滚筒在间隙正中公转 |
| 间隙分配 | 滚筒左侧空隙 = 右侧空隙 = `clearanceGap / 2` | 均等分配 |

### 2.4 材料密度（来源：xlsx 密度计算）

| 设备 | 层数 | DNA 模板基质量 | 公式 | 材料密度顺序 | 质量占比 |
|---|---|---|---|---|---|
| SEG | 4 | 30 | `M = 30 + 4m` | 集电3.25/阀1/磁2.5/发射1.75 | 40/10/30/20% |
| IGV | 6 | 105 | `M = 105 + 6m` | 内核6/集电1/阀5/磁2/发射4/外壳3 | 28.6/4.8/23.8/9.5/19/14.3% |

有效密度 `ρ = Σ(layerMass) / Σ(layerMass / layerDensity)`

---

## 三、9 种几何方案详解

### 方案1：已知尺寸求体积（ctrl=size）
- **驱动参数**：Yr, Br₀, n, e系数（全部可调）
- **公式**：`BR = Yr × num × n / 2`, `Brᵢ₊₁ = BRᵢ + su + sd + 2×Yr`, `Bh = D×e`, `Yh = min(D/tan22.5°, Bh/k)`
- **e系数**：生效（Bh 和 Yh 均由 e 驱动）

### 方案2：已知体积求高度（ctrl=volume）
- **驱动参数**：目标定子体积 Bv, Yr
- **公式**：`Bh = Bv / (π(BR²−Br²))`, `Yh = max(1, min(Bh/k, Bh/1−1.2))`
- **e系数**：不生效（高度由体积驱动，非 e）

### 方案3：已知参数求 b 系数（ctrl=size）
- 同方案1，预留 b 系数计算扩展

### 方案4：体积等差 + 等厚（ctrl=size, nFac=2）
- **nFac 固定为 2**：`BR = Yr × num × 2 / 2 = Yr × num`（用户可覆盖 n 滑条，默认 2）
- **等厚约束（软）**：`t = BR₀ − Br₀`, `Brᵢ = max(Brᵢ, BRᵢ − t)`，间隙优先

### 方案5：等差 + 等厚等间距（ctrl=size）
- **n 可调**（默认 1）
- **等间距约束（软）**：`g₀=Br₁−BR₀, g₁=Br₂−BR₁` → 取平均 → 均匀分配，间隙 ≥ `2Yr+gap` 时生效
- **等厚约束（软）**：同方案4，执行顺序：先等间距 → 后等厚 → 间隙复查

### 方案6：三角函数定子半径（ctrl=size）

**核心公式**：
```
BRᵢ = (2×Yr + n) / (2 × sin(π / numᵢ))
```

**数学推导（正多边形弦长法）**：
将 `num` 个滚筒均匀分布一圈，相邻圆心弦长 = `2 × orbit × sin(π/num)`。
假设相切：`2×Yr = 2 × orbit × sin(π/num)` → `orbit = Yr/sin(π/num)` → `BR = (2Yr+n)/(2sin(π/num))`。

n=12 时 `sin(π/12)≈0.259`（BR 比方案1~5 大 ~15%），n=32 时 `sin(π/32)≈0.098` 逼近弧长公式。

### 方案7：质量密度体积（ctrl=mass）
- **驱动参数**：三环各自质量 mᵢ, 密度 ρ
- **公式**：`Bv = mᵢ/ρ`, `Bh = Bv / (π(BR²−Br²))`, `Yh = max(1, min(Bh/k, Bh/1−1.2))`
- **e系数**：不生效

### 方案8：等体积质量模式（ctrl=mass）
- **驱动参数**：三环总质量 m₀+m₁+m₂, 密度 ρ
- **公式**：`Bv = (m₀+m₁+m₂) / (3ρ)`, 三环共用同一 Bv → `Bhᵢ = Bv / (π(BRᵢ²−Brᵢ²))`
- **效果**：内环面积最小 → Bh 最高，外环面积最大 → Bh 最低，自动满足高度排序
- **e系数**：不生效

### 方案9：等体积 + sp=质量数 + rp=P34（ctrl=mass）
- **驱动参数**：同方案8（三环总质量 m₀+m₁+m₂, 密度 ρ）
- **sp**：定子磁极数由 `massOverrides.m`（质量推荐）驱动，非滑条
- **rp**：滚筒磁极数固定为 `[34, 34, 34]`（P34 统一）
- **cfgSelect**：禁用，质量推荐自动填充
- **效果**：几何与方案8相同（等体积），但磁极数由 CSV 质量推荐确定，配合 P34 实现评分最大化
- **e系数**：不生效

### 方案对比速查

| 方案 | BR公式 | 高度驱动 | nFac | 等厚 | 等间距 | e生效 | sp来源 |
|---|---|---|---|---|---|---|
| 1 | Yr·num·n/2 | D×e | 1(可调) | — | — | ✅ | 滑条 |
| 2 | Yr·num·n/2 | Bv/面积 | 1(可调) | — | — | ❌ | 滑条 |
| 3 | Yr·num·n/2 | D×e | 1(可调) | — | — | ✅ | 滑条 |
| 4 | Yr·num·n/2 | D×e | **2**(可覆盖) | ✅ | — | ✅ | 滑条 |
| 5 | Yr·num·n/2 | D×e | 1(可调) | ✅ | ✅ | ✅ | 滑条 |
| 6 | (2Yr+n)/(2sin(π/num)) | D×e | 1(可调) | — | — | ✅ | 滑条 |
| 7 | Yr·num·n/2 | m/(ρ·面积) | 1(可调) | — | — | ❌ | 滑条 |
| 8 | Yr·num·n/2 | (Σm)/(3ρ·面积) | 1(可调) | — | — | ❌ | 滑条 |
| 9 | Yr·num·n/2 | (Σm)/(3ρ·面积) | 1(可调) | — | — | ❌ | massOverrides.m |

---

## 四、全局约束与 9 步清洗流水线（所有方案强制，严格按序执行）

所有 BR/Br 计算完成后，统一执行以下 9 步约束：

| 步骤 | 约束 | 修正动作 | 说明 |
|---|---|---|---|
| ① 正厚度 | `BR[i] ≥ Br[i] + 1` | `BR[i]=Br[i]+1` | 防环反翻 |
| ② 间隙强制 | `Br[i+1] ≥ BR[i] + 2·Yr + clearanceGap` | 推大 `Br[i+1]` | 滚筒放得进 |
| ③ 后环正厚度 | `BR[i] ≥ Br[i] + 1`（步骤②推大了 Br） | 跟进扩大 `BR[i]` | 后环不反翻 |
| ④ **厚度排序** | `t₀ ≥ t₁ ≥ t₂`（`tᵢ=BR[i]−Br[i]`） | 缩 `Br[i]` 满足，软约束 | **全方案通用** |
| ⑤ 方案5等间距 | `Br[1]−BR[0] = Br[2]−BR[1]` | 取平均均匀分配 | 仅方案5 |
| ⑥ 方案4/5等厚 | `Br[i] = max(Br[i], BR[i]−t)`（`t=BR[0]−Br[0]`） | 软约束，间隙优先 | 仅方案4/5 |
| ⑦ 间隙复查 | `Br[i+1] ≥ BR[i] + 2·Yr + clearanceGap` | 推大 `Br[i+1]` | 等厚/等间距后再次验证 |
| ⑧ **绝对排序** | `Br[0]<BR[0]<Br[1]<BR[1]<Br[2]<BR[2]` | 最少 0.5cm 梯差强制修正 | 最后防线 |
| ⑨ 轨道重算 | `orbit[i] = (BR[i] + Br[i+1])/2` | 基于修正后 Br/BR | 滚筒在间隙正中 |

### 高度层约束（BR/Br 几何清洗之后）

| 约束 | 公式 | 执行顺序 |
|---|---|---|
| 每环滚筒高≤定子高 | `hR[i] = min(hR[i], hS[i])` | ① 先执行 |
| 高度排序 | `hS₀≥hS₁≥hS₂`, `hR₀≥hR₁≥hR₂` | ② 后排序 |

### 滑条层硬限制

| 约束 | 公式 | 控制 |
|---|---|---|
| 间隙下限 | `gap ≥ 2×Yr + clearanceGap` | 间隙(cm) 1~5，默认 1 |
| k1 比例约束 | `hR ≤ hS/k1`（公式 + 滑条截断） | k1(约束比) 1~1.5，默认 1.2 |
| k2 间隙约束 | `hR = max(1, hS−2×k2)` | k2(间隙) 0~9cm，默认 1cm |
| k1/k2 二选一 | 下拉切换，实时生效 | 滚筒高约束下拉 |
| V定子环min | `min(定子体积₀,₁,₂) ≥ threshold` | V定子环min(cm³) 1~200 |
| 滚筒数量 | `num₀ < num₁ < num₂`（滑条自动修正） | 硬限制 |
| Br0 范围 | 0~2000cm，step=0.5 | 默认 0 |
| 滚筒片质量 | 8片/滚筒，1~120g/片 | 默认 34g（8×34=272g/颗） |
| 滚筒最小体积 | `min(单颗滚筒体积) ≥ minRollerVol` | 滚筒最小体积cm³ 0.1~30，默认 3 |
| 全极数直绘 | ☑勾选后 `vis=nP` 跳过 unifiedVis 压缩 | 全局勾选框，默认不勾 |

### e 系数生效范围

| 方案 | 定子高 Bh | 滚筒高 Yh | e 影响 |
|---|---|---|---|
| 1,3,4,5,6 | `Bh = D×e` | `Yh = min(D/tan22.5°, Bh/k)` | ✅ 定子+滚筒 |
| 2,7,8 | 体积/质量驱动 | 由 Bh 反推 | ❌ 不生效 |

---

## 五、nb 程序设计溯源

原始 Wolfram nb 程序的核心设计：
1. **relation.nb** — 定义约束方程，求解 BR, Br, 高度关系
2. **index-滑动条备份.nb** — 用 `Manipulate` 实现交互式滑块
3. **Solve 求解**：从体积反推半径：`Solve[π(BR²−Br²)×Bh == V, Br]`
4. **轨道公式**：`orbit₁ = (BR₁+Br₂)/2`, `orbit₂ = (BR₂+Br₃)/2`
5. **摆线轨迹**：`Epitrochoids` 函数绘制滚筒绕定子轨迹
6. **转速比**：基于面积/极数比，经验值约 2.5×（网页采用固定 2.5）

本网页引擎在 nb 基础上增加了：文档物理公式（tan22.5°, e 系数）、CSV 质量推荐、间隙可视化、正视图/俯视图双卡片。

---

## 六、CSV 数据源

| 文件 | 对应设备 | 列含义 |
|---|---|---|
| `seg.csv` | SEG(4层) | 第1环质量, 第2环质量, 第3环质量, 公差, GCD |
| `igv.csv` | IGV(6层) | 同上 |

前三列同时可作为定子磁极数使用。质量推荐下拉在方案7/8/9时自动将 CSV 值填入质量滑条并计算材料密度。方案9中 sp 由 `massOverrides.m` 驱动（非滑条）。

---

## 七、视图说明

### 7.1 画布自适应
```
W = min(window.innerWidth − 32, 1400)  // 1400px 上限
H1 = W                                   // 俯视 = 正方形
H2 = max(W × 0.4, hS₀ × 3.2 + 60)      // 正视图自适高
scale = W × 0.45 / maxR                  // 俯视+正视统一缩放
```
- **📷截长图**：点击绿色按钮 → html2canvas 2倍高清 → 整页 PNG 自动下载

### 7.2 定子极显示：unifiedVis（统一因子算法）

旧版使用 `bestDivisor(nP, 360)` 为每个环独立选择可显示段数。新版升级为 **unifiedVis(sp)**：

```
unifiedVis(sp₀, sp₁, sp₂):
  g = gcd(sp₀, sp₁, sp₂)       // 三环真正的 GCD
  若 g > 1:
    vis = 最大因子 ≤360 且 ≥60
  否则 (g = 1):
    收敛到最接近中位数的因子:
      med = 中位数(sp₀, sp₁, sp₂) 的因子
      选 ≤360 ≥60 的最大因子
  step = nP / vis（必为整数，无舍入）
```

**关键**：三环共享同一个 `vis` 值，保证在俯视图上显示**相同数量**的 N-S 段。`step = nP/vis` 始终为整数。

- **俯视图（卡片1）**：W×W 正方形，定子=环形材料层+红蓝磁极段（unifiedVis），滚筒=极点圆+公转轨道虚线+轨迹线可选
- **正视图（卡片2）**：宽度=W，高度自适应。定子=左右对称矩形条，滚筒=矩形居中悬浮，共用中线，间隙灰色带可视，中心虚线。8 段滚筒片在 info 行显示

---

> ⚠️ **重要声明 / Important Disclaimer**
> 
> 本文档由 AI 辅助生成，部分结论可能存在 AI 幻觉导致的论证不严谨之处。
> 文中提出的数学、物理及相关跨学科观点，需要经过专业数学家、物理学家
> 及相关领域专家共同验证与检验。
> 如有疏漏、错误或不同见解，敬请指正，不胜感激。
> 
> **This document was AI-assisted. Some conclusions may contain inaccuracies
> due to AI hallucination. All mathematical, physical, and interdisciplinary
> claims require verification by professional mathematicians, physicists,
> and subject-matter experts. Corrections and feedback are warmly welcomed.**
