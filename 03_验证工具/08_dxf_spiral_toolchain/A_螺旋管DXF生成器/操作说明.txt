在pycharm里面运行 dxf转点数据口朝右上.py 用这个程序把这个“your_curve口朝右上.dxf”文件转换成csv文件，注意输出的文件名要改成如下： spline_points口朝右上.csv
然后运行在pycharm里面运行最简原始模版用于生成直锥螺旋管.py 会生成如下文件《最简优先 对扭转—口朝内(头朝下).dxf》，螺旋锥 对扭转—口朝内(头朝下).dxf文件，大家可以导入到你们的rhino软件或者其他支持导入dxf的建模软件。
不懂代码逻辑原理的，可以直接复制代码问chatgpt-O3或者deepseek-R1。把代码复制到对话框问它：我不懂这个代码，请问这个数学逻辑和工序是怎样的，请解释一下。然后它会解读。
你可以让它修改其中的参数，以满足你的要求。可以换螺旋线的方程，断面的首尾大小amp参数（self.a = np.linspace(self.amp（首）, self.amp / 2.5（尾）, self.num_t)），
self.r=sign_spiral_dir:  ±1，控制螺旋线的旋转方向（1 或 -1），分别代表从top顶视图看是逆时针下旋或顺时针下旋。
sign: ±1，控制初始断面方向，-1可以让your_curve口朝右上.dxf这个断面旋转180°，导致管看似旋转了180°。
def __init__(self, spline_data_path, t_min=2 * np.pi, t_max=7 * np.pi, d=15, z_v=5, amp=1.5, sign_spiral_dir=1, sign=1, twist=1):
    大家可以修改上行的默认参数。
更多参数调整举例：可以调整第181行的代码：
B_term1 = self.sign * x(j) * B_unit[i]   ##sign设置为-1
N_term1 = self.sign * y(j) * N_unit[i] ##可以删除这行的“self.sign *”让断面关于断面原坐标的y轴镜像。
同理：
B_term1 = self.sign * x(j) * B_unit[i]  ##可以删除这行的“self.sign *”让断面关于断面原坐标的x轴镜像。
N_term1 = self.sign * y(j) * N_unit[i]  ##sign设置为-1
self.num_t这个参数调整螺旋线上的散点的数量。注意我的程序为350它是可以被7整除的。
第201行的代码：if i == 0 or (i + 1) % 7 == 0:   ##注意此处的7是因为以上350它是可以被7整除的。此行代码作用为了螺旋线的首尾都保证会有一个断面生成。
twist为±1控制断面的扭转方向，也就是决定管从首到尾是怎么扭转的，顺时针还是逆时针扭转。
    def update_psi(self):
        """更新psi值"""
        self.psi = np.linspace(0, self._twist * self.t_length / 2, self.num_t)  此处self.t_length / 2，代表管首尾扭转的度数为螺旋线旋转周期的一半。螺旋线旋转周期默认为self.t_length = t_max - t_min，
t_min=2 * np.pi, t_max=7 * np.pi，所以螺旋线旋转周期为self.t_length =5pi，除以2就是2.5pi。这个大家可以调整。如果把除以2改成×2，那么扭转会剧烈得有些像火龙卷风那种。

    def calculate_spiral_coords(self):
        """计算螺旋线的坐标"""
        x = np.exp(self.b * self.t_spiral) * self.d * np.sin(self.t_spiral) * self.r
        y = np.exp(self.b * self.t_spiral) * self.d * np.cos(self.t_spiral)
        z = np.exp(self.b * self.t_spiral) *(self.t_spiral- self.t_min) * self.z_v
        return x, y, z
这一段代码就是螺旋线的公式，你可以给一个不同的公式，让gpt给你以上这种格式。